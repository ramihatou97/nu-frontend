# NeuroSynth Frontend - Environment Configuration
# ===================================================

# Backend API URL (required)
# ----------------------------
# This is the base URL for the NeuroSynth backend API.
#
# Development (local):
VITE_API_BASE_URL=http://localhost:8000
#
# Production:
# VITE_API_BASE_URL=https://api.yourdomain.com
#
# Staging:
# VITE_API_BASE_URL=https://api.staging.yourdomain.com

# Usage Instructions:
# -------------------
# 1. Copy this file to `.env` for development:
#    cp .env.example .env
#
# 2. For production builds, create `.env.production`:
#    echo "VITE_API_BASE_URL=https://api.yourdomain.com" > .env.production
#
# 3. Run development server:
#    npm run dev
#
# 4. Build for production:
#    npm run build
#    (Uses .env.production if it exists, otherwise .env)

# Note: All environment variables must be prefixed with VITE_
# to be accessible in the frontend code via import.meta.env
